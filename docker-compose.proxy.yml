services:
  mongo:
    image: "mongo:latest"
    environment:
      - MONGO_INITDB_ROOT_USERNAME=root
      - MONGO_INITDB_ROOT_PASSWORD=password

  flagify:
    image: "flagify/flagify:latest"
    ports:
      - "3000:3000"
      - "3100:3100"
    depends_on:
      - mongo
    environment:
      - MONGODB_URI=mongodb://root:password@mongo:27017/flagify?authSource=admin
      # Proxy settings
      - PROXY_ENABLED=1
      - PROXY_HOST_INTERNAL=http://proxy:3300
      - PROXY_HOST_PUBLIC=http://localhost:3300
      # Change this to something secure in production!
      - SECRET_API_KEY=dev
    volumes:
      - uploads:/usr/local/src/app/packages/back-end/uploads

  proxy:
    image: "flagify/proxy:latest"
    ports:
      - "3300:3300"
    depends_on:
      - flagify
    environment:
      - FLAGIFY_API_HOST=http://flagify:3100
      # This must match the SECRET_API_KEY above
      - SECRET_API_KEY=dev

volumes:
  uploads:
